{
  "title": "module_rubric-properties_schema",
  "description": "schema des propriétés du module rubric",
  "type": "object",
  "properties": {
    "rubric_animation": {
      "description": "animation lors de l'apparition de la rubric",
      "type": "string",
      "input": "select-animation"
    },
    "rubric_timeline": {
      "description": "timeline properties",
      "type": "object",
      "properties": {
        "primary_color": {
          "description": "primary color",
          "type": "string",
          "input": "color-picker"
        },
        "secondary_color": {
          "description": "secondary color",
          "type": "string",
          "input": "color-picker"
        }
      },
      "required": [
        "primary_color",
        "secondary_color"
      ]
    }
  },
  "required": [
    "rubric_animation",
    "rubric_timeline"
  ]
}
